Got it — you're looking to match the sidebar height with the form panel and scale the progress icons and text proportionally so everything feels cohesive.

Here’s how you can accomplish that:

✅ Update the Main Layout to Match Form Height
In your main container (client/src/pages/free-assessment.tsx), make sure the layout wrapping both the sidebar and form has a minimum height that matches the form:

jsx
Copy
Edit
<div className="flex min-h-screen items-stretch bg-gray-50">
  {/* Sidebar */}
  <aside className="w-[300px] bg-[#0d2342] text-white flex flex-col">
    <ProgressSidebar />
  </aside>

  {/* Form */}
  <main className="flex-1 p-10">
    {/* Form content goes here */}
  </main>
</div>
✅ Sidebar: Match Height + Scale Content
Inside ProgressSidebar, use this updated layout:

jsx
Copy
Edit
<div className="flex flex-col h-full justify-between py-10 px-6">
  {/* Logo Section */}
  <div className="mb-6">
    <div className="bg-gradient-to-br from-[#1c3d4f] to-[#246a99] rounded-xl p-4 shadow-xl backdrop-blur-sm ring-1 ring-white/20">
      <img
        src={_3}
        alt="Apple Bites Business Assessment"
        className="w-full object-contain"
      />
    </div>
    <div className="mt-4 text-center text-sm font-semibold uppercase tracking-wide text-gray-300">
      Professional Valuation
    </div>
  </div>

  {/* Steps */}
  <div className="flex-grow flex flex-col justify-evenly gap-6">
    <ProgressIndicator />
  </div>

  {/* Optional Footer or Support Info */}
  <div className="text-xs text-gray-400 text-center mt-6">
    &copy; 2025 Meritage Partners
  </div>
</div>
✅ ProgressIndicator Component: Enlarge Icons & Text
Update your step items like this:

jsx
Copy
Edit
<div className={`flex items-center gap-3 text-base font-medium ${isCurrent ? 'text-white' : 'text-gray-400'}`}>
  <div className={`rounded-full p-3 ${isCurrent ? 'bg-blue-600' : 'bg-gray-600'}`}>
    <Icon className="w-6 h-6" />
  </div>
  <div>
    <div className="text-lg">{label}</div>
    <div className="text-sm font-light">{`Step ${index + 1}`}</div>
  </div>
</div>
Consider bumping text-lg and w-6 h-6 up to text-xl and w-7 h-7 if still too small.

🧩 Optional – Match Form Border Radius
If your form container has a large border-radius (rounded-2xl or similar), you may also want to apply the same to the sidebar’s edge:

jsx
Copy
Edit
<aside className="w-[300px] bg-[#0d2342] text-white flex flex-col rounded-l-2xl shadow-lg">