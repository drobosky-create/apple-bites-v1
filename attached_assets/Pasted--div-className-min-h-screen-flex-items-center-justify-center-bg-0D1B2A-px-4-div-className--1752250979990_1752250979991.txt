<div className="min-h-screen flex items-center justify-center bg-[#0D1B2A] px-4">
  <div className="w-full max-w-md bg-[#1B263B] rounded-xl shadow-xl p-8 space-y-6">

    {/* Logo and Heading */}
    <div className="text-center">
      {/* Replace this with your actual logo if needed */}
      <div className="text-white text-3xl font-bold flex justify-center items-center gap-2 mb-2">
        <span role="img" aria-label="apple">🍎</span> Apple Bites
      </div>
      <h2 className="text-white text-2xl font-bold">Welcome Back to Apple Bites</h2>
      <p className="text-[#E0E1DD] text-sm mt-1">Access your valuation dashboard below</p>
    </div>

    {/* Login / Register Toggle */}
    <div className="flex justify-between bg-[#415A77] rounded-lg text-white font-medium text-sm overflow-hidden">
      <button className="w-1/2 py-2 bg-[#415A77] hover:bg-[#778DA9] transition">Login</button>
      <button className="w-1/2 py-2 hover:bg-[#778DA9] transition">Need an Account?</button>
    </div>

    {/* Form */}
    <form onSubmit={loginForm.handleSubmit(onLoginSubmit)} className="space-y-4">

      {/* Email */}
      <div className="space-y-2">
        <Label htmlFor="email" className="text-white font-semibold">Email</Label>
        <Input
          id="email"
          type="email"
          {...loginForm.register("email")}
          placeholder="Enter your email address"
          className="bg-[#1B263B] text-white placeholder-[#E0E1DD] border border-[#415A77] rounded-lg focus:ring-2 focus:ring-[#778DA9]"
        />
      </div>

      {/* Password */}
      <div className="space-y-2">
        <Label htmlFor="password" className="text-white font-semibold">Password</Label>
        <div className="relative">
          <Input
            id="password"
            type={showPassword ? "text" : "password"}
            {...loginForm.register("password")}
            placeholder="Enter your password"
            className="bg-[#1B263B] text-white placeholder-[#E0E1DD] border border-[#415A77] rounded-lg focus:ring-2 focus:ring-[#778DA9]"
          />
          {/* Add visibility toggle button if needed */}
        </div>
      </div>

      {/* Sign In Button */}
      <Button
        type="submit"
        className="w-full text-white font-semibold bg-[#415A77] hover:bg-[#778DA9] transition duration-200 rounded-lg"
        disabled={loginMutation.isPending}
      >
        {loginMutation.isPending ? "Signing In..." : "Sign In"}
        <LogIn className="ml-2 h-4 w-4" />
      </Button>
    </form>

  </div>
</div>
